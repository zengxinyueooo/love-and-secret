# Love & Secret —— 技术里程碑详解（面试叙事）

> 本文档按里程碑累加，每个阶段记录：做了什么、亮点、选型理由、方案对比、最终效果。
> 项目定位：面向人机恋/AI 陪伴场景的长期记忆系统，核心矛盾是「情感记忆的完整性」与「LLM 上下文的有限性」。

---

## M1 后端底座：Node/Hono + Drizzle + Supabase 多会话

### 做了什么

把纯前端 localStorage 架构（滚动 10 条窗口，无任何持久记忆）升级为前后端分离：

```
Vue3 前端 ⇄ Node/Hono 单服务 (8787) ⇄ Supabase (PostgreSQL, 首尔节点)
```

- **server/** 子项目：Hono + TypeScript + Drizzle ORM + zod 校验
- conversations / messages 两张表，消息带 `turnIndex` 全局序号（后续章节蒸馏的切割依据）
- 多会话 CRUD：创建/改名/删除（级联删消息）、消息增量拉取（`?after=`）
- 前端 chat store 重写：多会话切换 + **localStorage 降级**（后端离线不阻断聊天）+ 旧聊天记录首次连接自动导入为新会话

### 选型与对比

| 决策 | 选择 | 为什么 | 对比方案的问题 |
|---|---|---|---|
| 后端语言 | Node + TypeScript | 与前端同语言，全栈一套类型定义；机器零额外安装 | Spring Boot：JVM 生态重，个人项目杀鸡用牛刀；Go/Python：跨语言成本 |
| 框架 | Hono | TS 原生、Web Standard API（fetch/Request/Response），未来可无改动迁 Cloudflare Workers/Bun | Express：回调风格老、TS 后补；NestJS：DI+装饰器全家桶对这个体量是过度设计 |
| ORM | Drizzle | schema 即 TS 代码、类型安全、生成的 SQL 可预测 | Prisma：引擎二进制重、迁移体系黑盒 |
| 数据库 | Supabase（托管 PG） | 免安装免运维、内置 pgvector（M4 检索直接用）、免费档够用、随时全量 SQL 导出（数据主权） | 本地自装 PG：Windows 装 pgvector 繁琐且只能本机演示；MongoDB：无关系约束、pgvector 检索能力缺失 |

### 亮点

1. **降级设计**：后端不可达时前端自动回落 localStorage，聊天永不中断——可用性优先于一致性，且切回来时数据不丢。
2. **数据迁移的连续性**：用户的旧 localStorage 聊天记录自动导入后端，架构升级对用户零感知。
3. **安全细节**：`.env`（数据库密码）与 `.workbuddy/` 进 `.gitignore`；API Key 不落库，由前端每轮 Header 传入。

### 效果

多会话、全量消息落库 PostgreSQL，消息按序持久化，前端任意刷新/换设备数据不丢。为记忆系统提供了唯一可信数据源（single source of truth）。

---

## M2 Context 装配器：七层 Prompt 组装 + LLM Gateway

### 做了什么

把「发给模型的上下文」从前端拼字符串（最近 10 条 + system prompt）升级为**服务端确定性装配**：

```
浏览器 → POST /api/conversations/:id/chat
              ↓ 服务端
        1. 用户消息落库（turnIndex 递增）
        2. Context 装配器组装七层 prompt
        3. LLM Gateway 流式调用（超时/重试/空产出检测）
        4. 助手消息落库，meta 写入 Context Trace
              ↓ SSE 流（delta / done / error）
        浏览器逐字渲染
```

**七层 Context 模型**（`server/src/services/context-assembler.ts`）：

| 层 | 内容 | 状态 |
|---|---|---|
| L0 Persona | 会话人设卡（服务端存储，唯一事实来源） | ✅ 实装 |
| L1 Relationship | 关系状态（亲密度/阶段） | M5 |
| L2 Ongoing | 进行中的事（话题/承诺） | M5 |
| L3 Recent | 近期对话原文（滚动窗口 24 条，预算 6000 字符） | ✅ 实装 |
| L4 Summary | 章节摘要（Chapter Distillation） | M3 |
| L5 Retrieved | 检索命中的长期记忆（混合检索） | M4 |
| L6 Lore | 世界书/设定补充 | M6 |

每层**固定注入位置**（区块标题如`【你是谁】`）+ **固定字符预算**（LAYER_BUDGET）+ 装配产出 **Context Trace**（注入了哪些层、字符数、耗时、装配器版本号）写入消息 meta。

**LLM Gateway**（`server/src/services/llm-gateway.ts`）：三级模型路由——chat（温度 0.8）/ extraction（0.2）/ eval（0），各自独立超时与重试次数；指数退避；4xx（除 429）不重试；**空产出检测**。

### 核心决策：确定性注入 vs Tool Use（函数调用）

这是本项目最重要的架构决策。替代方案是给模型 `search_memory` 工具让它自己检索记忆：

- LLM 决定「调不调」是概率行为，不可复现——今天记得、明天忘
- 工具调用引入额外轮次，延迟翻倍
- 失败无法归因：没调？调了没命中？命中没用上？全是黑盒

确定性装配的收益：行为可复现（可做 A/B 评测）、Trace 全程可观测（问题归因到层）、记忆命中质量只取决于检索算法本身。

> 一句话：陪伴场景的记忆必须 100% 可靠——用户对「他忘了我们说过的话」容忍度是零。我把「是否记得」从概率问题变成了工程问题。

### 其他选型对比

- **SSE vs WebSocket**：聊天只需单向推流，SSE 建立在 HTTP 上（Hono 原生 streamSSE、断线自动重连），WebSocket 的双向能力在此场景是纯开销。
- **固定预算 vs 全塞上下文**：Lost in the Middle 问题——LLM 对上下文中部信息利用率显著偏低，无限堆上下文既贵又稀释关键信息；固定预算强制取舍且成本上限可预测。
- **Trace 带版本号**：不同版本装配器的产出可分组对比，M6 benchmark 的实验基础。

### 踩坑经验（来自真实失败案例调研）

个人开发者实测：Claude 遇敏感内容会**静默返回空内容**，导致情感浓度最高的记忆反而最容易被丢。因此 Gateway 把空产出定义为错误并触发重试——这是第一道防线，M3 提取管道的产出校验是第二道。

### 效果

- 每轮请求模型看到的 prompt 由服务端确定性生成，人设永远在场（与窗口分属不同层，不会被挤掉）
- 全部消息服务端落库，Trace 完整记录「模型看到了什么」
- 失败路径真实验证：401 → SSE error 事件 → 用户消息不丢
- 五层占位接口就位，M3-M6 每个里程碑 = 实现一层 + 接入装配器

---

## M3 记忆提取管道：Turn 级提取 + 章节蒸馏 + Write Gate

### 做了什么

记忆从「存对话原文」进化为「存结构化记忆」。三个组件：

**1. Turn 级提取**（`server/src/services/extraction.ts`）
每轮聊天结束后异步触发（不阻塞聊天流）：把这轮 user+assistant 交换交给提取模型（温度 0.2），产出结构化 JSON，写入 `memories` 表。四类记忆：

| 类型 | 内容 | 示例 |
|---|---|---|
| fact | 用户稳定事实/偏好 | 「她怕黑，睡觉要开小夜灯」 |
| episode | 具体事件（带原文回链 sourceMessageIds） | 「周六晚上一起看了流星雨」 |
| emotion | 情绪状态及原因 | 「她因为项目延期很焦虑」 |
| event | 关系事件 | 「约定下个月一起过纪念日」 |

**2. Chapter Distillation**（`server/src/services/distillation.ts`）
每累计 24 条消息（约 12 轮）触发：把这章原文蒸馏成 200-400 字章节摘要写入 `chapters` 表，经装配器注入 L4 摘要层（最近 3 章）。`messageIds` 保留回链。

**3. Write Gate 分级写入**（human-in-the-loop）
- importance ≥ 0.6 且 confidence ≥ 0.7 → 自动写入（active）
- 其余 → `pending_review`，通过 `PATCH /api/memories/:id` 批准（approve）/ 拒绝（reject）/ 编辑（edit）

### 防坑三件套（来自真实失败案例，面试的核心素材）

| 坑（来源） | 防线 |
|---|---|
| 压缩过度：一晚上的事被压成 14 个字，情感细节全丢（个人开发者实测） | episode 字数下限 15 字，不足拒绝；蒸馏摘要 < 60 字视为失败放弃本次；**所有记忆带 sourceMessageIds 原文回链**——摘要丢了也能取回原文 |
| 敏感内容静默跳过：Claude 返回空导致最重要的记忆被丢（个人开发者实测） | Gateway 空产出抛错（M2 第一道）+ 提取结果回写 assistant 消息 meta（M3 第二道）——失败可见、可追溯，绝不静默丢失 |
| JSON 产出被 markdown 包裹/污染 | 解析前剥 fence、截取 [ ] 区间；不合法换更严格提示重试一次 |

### Temporal Supersession（时态失效）

新旧事实矛盾时**不删旧记录**：旧记录 `status='superseded' + validTo=now`，新记录正常写入。关系是会演进的（「喜欢喝拿铁」→「最近改喝美式了」），删除会让「他为什么记得我以前爱喝拿铁」这种历史感消失。M3 用字符二元组 Jaccard ≥ 0.45 做轻量冲突检测，M4 换 embedding 余弦相似度。

### 为什么 24 条一章

- < 20：蒸馏频繁、成本高、摘要碎片化丢叙事连续性
- \> 40：近期窗口（24 条）滑出后到下次蒸馏之间存在「遗忘空窗」
- 24 是窗口大小 = 无缝衔接：原文滑出窗口的同时恰好进入章节摘要

### 提取为什么异步、为什么放服务端

- **异步**（`void runExtraction(...)` 不 await）：提取增加 1-3 秒延迟，用户不该等；聊天流走 SSE 结束后触发
- **服务端**：和 M2 同理——提取是记忆系统的职责，不该依赖前端是否开着页面；提取用的 LLM 凭证由本轮请求 Header 透传，不落库

### 效果（冒烟验证）

- 假 key 提取失败 → 错误完整记入 assistant 消息 meta（`{error: "LLM API 401..."}`），主流程零影响
- Write Gate 全链路：pending_review 列表 → approve 变 active → reject 变 rejected
- 一轮真实对话完成后（配好 key）：`GET /api/memories` 能看到提取出的记忆，聊满 12 轮后 L4 摘要层开始注入

---

## M4 混合检索：向量 + 全文 + 时序，RRF 融合

### 做了什么

让 L5 Retrieved 层真正具备「从成百上千条记忆里召回真正相关的那几条」的能力。三路召回 + 融合：

```
                  用户当前输入
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   通道 A 向量       通道 B 全文       （无第三通道——
   pgvector <=>       tsvector @@        时间衰减 & importance
   余弦相似度        websearch_to_tsquery  作为融合权重参与）
        │              │
        └──────┬───────┘
               ▼
          RRF 融合 (Cormack 2009, k=60)
               ▼
     score × exp(-age/half_life) × (0.5 + 0.5×importance)
               ▼
         top 8 注入 L5 Retrieved 层
```

### 三路召回 vs 单一检索的选型对比

| 通道 | 擅长 | 不擅长 | 实现 |
|---|---|---|---|
| **向量**（pgvector HNSW） | 同义改写（「开夜灯」↔「怕黑睡不好」）、语义关联 | 专有名词、数字、纪念日日期 | `embedding <=> query_vec` 余弦 |
| **全文**（PostgreSQL tsvector + websearch_to_tsquery） | 字面命中（「三周年」「纪念日」）、低延迟（不需要 embedding API） | 改写、抽象表达 | `ts_rank_cd(content_tsv, query)` |
| **重要性 + 时间** | 让旧/不重要记忆自然下沉 | 不能独立召回 | 融合权重 `importance × exp(-age/30天)` |

**为什么必须混合**：纯向量会让"周年纪念日"这种数字串被淹没（embedding 把 "2024-03-14" 和普通文本混在一起）；纯全文会让"开夜灯"和"怕黑"永远凑不到一起——互补关系。

**为什么是 RRF 不是加权打分**：RRF 只用「排名」做融合，不需要对向量分数和 BM25 分数做归一化（归一化是工程黑洞，每换一次向量就要重调权重）。RRF 公式 `Σ 1/(k+rank_i)` 把多路排名降序相加，对每路单独不敏感，工程鲁棒性显著更好。

### 时序衰减设计

记忆不是越久越值钱，但也不是「一刀切按时间删」。半衰期 30 天 + importance 调节：

- `weight = rrf_score × exp(-age_days / 30) × (0.5 + 0.5 × importance)`
- importance=1.0 的记忆：3 个月后权重降到 25%，但仍能召回
- importance=0.3 的寒暄：3 个月后权重降到 5%，自然被排到后位
- 不直接删除——「他为什么记得我们刚开始聊过什么」这种历史感对情感陪伴很重要

### 工程实现细节

**1. Schema 层**（`server/src/db/schema.ts`）

```ts
// pgvector customType —— 让 Drizzle 能读写 vector(1536) 列
const embedding1536 = customType<{ data: number[]; driverData: string }>({
  dataType() { return 'vector(1536)' },
  toDriver(v) { return `[${v.join(',')}]` },
  fromDriver(s) { return s.slice(1, -1).split(',').map(Number) },
})
```

**2. SQL migration**（`server/drizzle/0002_hybrid_retrieval.sql`）

- `CREATE EXTENSION vector`（Supabase 默认开启）
- `embedding vector(1536)` 列 + HNSW 索引 `WITH (m=16, ef_construction=64)`
- `content_tsv tsvector GENERATED ALWAYS AS (to_tsvector('simple', content)) STORED` —— 生成列自动维护，无需应用层维护
- `GIN (content_tsv)` 全文索引

**为什么 HNSW 不要 IVFFlat**：IVFFlat 需要训练（先有数据再建索引），小数据集或新表会卡；HNSW 无训练即用，对 1k-100k 量级的记忆集合召回质量更好。

**为什么 tsvector 用生成列不用触发器**：生成列由 PG 自身维护，不可能漏触发、不可能不一致；触发器一旦应用逻辑挂了 tsvector 就脏了。

**3. Embedding 服务**（`server/src/services/embeddings.ts`）

- OpenAI 兼容协议 → 任何 OpenAI/Azure/SiliconFlow/智谱 endpoint 都能用
- 配置由前端 env 注入（`EMBEDDING_API_KEY` / `EMBEDDING_BASE_URL` / `EMBEDDING_MODEL`）—— 用户数据主权
- 单条 + 批量；超时 + 重试；空产出检测
- **优雅降级**：无 key 时不崩溃，retrieval 跳过向量通道只走全文

**4. 检索入口**（`server/src/services/retrieval.ts`）

`searchMemories(query, conversationId, topK=8)` 单一入口；每次返回 `{ items, trace }`，trace 含「向量通道命中数 / 全文通道命中数 / 总耗时 / embedding 是否可用」——为 M6 benchmark 铺路。

**5. 检索上下文装配**（`server/src/services/context-assembler.ts`）

L5 层从 null 占位升级为真实注入：

```
[fact] 她怕黑，睡觉要开小夜灯
[episode] 第一次见面是去年春天樱花季
[event] 约定下个月 14 号一起过纪念日
```

**trace 版本号 bump** `ctx-assembler/0.2 → 0.3`：M4 之后装配器产出形态变了，trace 用于 M6 不同版本装配器的对比评测。

**6. Debug 端点**

`GET /api/retrieval/search?conversationId=...&q=<query>&topK=8` —— 不依赖前端也能 curl 验证检索质量。

### 与 M3 的协同：Temporal Supersession 升级

M3 用字符二元组 Jaccard 检测事实冲突（无 embedding 的过渡方案）。M4 落地后，supersede 可以升级为 embedding 余弦相似度，准确性更高——但作为**可选优化**，M3 的字符方案在没有 embedding key 的降级路径里依然工作。

### 踩坑记录

| 坑 | 修复 |
|---|---|
| Drizzle 没有内置 vector 类型 | customType 适配，toDriver/fromDriver 处理 `[1,2,3]` 字符串格式 |
| `applyFinalRanking` 想 await 但用 `.then()` 返回 Promise 没 await | 类型系统兜住了，但运行时是 Promise；改回 `async/await` |
| 检索 trace 包含 embedding 状态 | 让前端能在零 embedding 配置时也确认「检索降级到了纯全文」 |

### 效果

- L5 Retrieved 层从「永远为空」变为「真正注入相关记忆」
- Context Trace 多了 `vector=Nhit, text=Nhit, duration=NNms` 的可观测数据
- 用户即使不配 embedding key，**纯全文通道也能工作**——渐进式启用
- 三路召回 + RRF 融合 + 时序衰减 + importance 加权，构成本项目最复杂的检索管线

### 一句话定位

> 我用确定性装配解决了"是否记得"；用混合检索解决了"记得多准"。两个里程碑合起来，把"他忘了我们说过的话"从产品缺陷变成了工程可控变量。

---

## M5 情感特化层：关系状态机 + 情绪坐标 + 遗忘曲线

（待完成）

---

## M6 可观测 + Eval：Context Trace 面板 + 小型 Benchmark

（待完成）

---

## M7 前端整合：记忆面板 + 数据导出

（待完成）
