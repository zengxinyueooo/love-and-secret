# Love & Secret —— 技术里程碑详解（面试叙事）

> 本文档按里程碑累加，每个阶段记录：做了什么、亮点、选型理由、方案对比、最终效果。
> 项目定位：面向人机恋/AI 陪伴场景的长期记忆系统，核心矛盾是「情感记忆的完整性」与「LLM 上下文的有限性」。

---

## M1 后端底座：Node/Hono + Drizzle + Supabase 多会话

### 做了什么

把纯前端 localStorage 架构（滚动 10 条窗口，无任何持久记忆）升级为前后端分离：

```
Vue3 前端 ⇄ Node/Hono 单服务 (8787) ⇄ Supabase (PostgreSQL, 首尔节点)
```

- **server/** 子项目：Hono + TypeScript + Drizzle ORM + zod 校验
- conversations / messages 两张表，消息带 `turnIndex` 全局序号（后续章节蒸馏的切割依据）
- 多会话 CRUD：创建/改名/删除（级联删消息）、消息增量拉取（`?after=`）
- 前端 chat store 重写：多会话切换 + **localStorage 降级**（后端离线不阻断聊天）+ 旧聊天记录首次连接自动导入为新会话

### 选型与对比

| 决策 | 选择 | 为什么 | 对比方案的问题 |
|---|---|---|---|
| 后端语言 | Node + TypeScript | 与前端同语言，全栈一套类型定义；机器零额外安装 | Spring Boot：JVM 生态重，个人项目杀鸡用牛刀；Go/Python：跨语言成本 |
| 框架 | Hono | TS 原生、Web Standard API（fetch/Request/Response），未来可无改动迁 Cloudflare Workers/Bun | Express：回调风格老、TS 后补；NestJS：DI+装饰器全家桶对这个体量是过度设计 |
| ORM | Drizzle | schema 即 TS 代码、类型安全、生成的 SQL 可预测 | Prisma：引擎二进制重、迁移体系黑盒 |
| 数据库 | Supabase（托管 PG） | 免安装免运维、内置 pgvector（M4 检索直接用）、免费档够用、随时全量 SQL 导出（数据主权） | 本地自装 PG：Windows 装 pgvector 繁琐且只能本机演示；MongoDB：无关系约束、pgvector 检索能力缺失 |

### 亮点

1. **降级设计**：后端不可达时前端自动回落 localStorage，聊天永不中断——可用性优先于一致性，且切回来时数据不丢。
2. **数据迁移的连续性**：用户的旧 localStorage 聊天记录自动导入后端，架构升级对用户零感知。
3. **安全细节**：`.env`（数据库密码）与 `.workbuddy/` 进 `.gitignore`；API Key 不落库，由前端每轮 Header 传入。

### 效果

多会话、全量消息落库 PostgreSQL，消息按序持久化，前端任意刷新/换设备数据不丢。为记忆系统提供了唯一可信数据源（single source of truth）。

---

## M2 Context 装配器：七层 Prompt 组装 + LLM Gateway

### 做了什么

把「发给模型的上下文」从前端拼字符串（最近 10 条 + system prompt）升级为**服务端确定性装配**：

```
浏览器 → POST /api/conversations/:id/chat
              ↓ 服务端
        1. 用户消息落库（turnIndex 递增）
        2. Context 装配器组装七层 prompt
        3. LLM Gateway 流式调用（超时/重试/空产出检测）
        4. 助手消息落库，meta 写入 Context Trace
              ↓ SSE 流（delta / done / error）
        浏览器逐字渲染
```

**七层 Context 模型**（`server/src/services/context-assembler.ts`）：

| 层 | 内容 | 状态 |
|---|---|---|
| L0 Persona | 会话人设卡（服务端存储，唯一事实来源） | ✅ 实装 |
| L1 Relationship | 关系状态（亲密度/阶段） | M5 |
| L2 Ongoing | 进行中的事（话题/承诺） | M5 |
| L3 Recent | 近期对话原文（滚动窗口 24 条，预算 6000 字符） | ✅ 实装 |
| L4 Summary | 章节摘要（Chapter Distillation） | M3 |
| L5 Retrieved | 检索命中的长期记忆（混合检索） | M4 |
| L6 Lore | 世界书/设定补充 | M6 |

每层**固定注入位置**（区块标题如`【你是谁】`）+ **固定字符预算**（LAYER_BUDGET）+ 装配产出 **Context Trace**（注入了哪些层、字符数、耗时、装配器版本号）写入消息 meta。

**LLM Gateway**（`server/src/services/llm-gateway.ts`）：三级模型路由——chat（温度 0.8）/ extraction（0.2）/ eval（0），各自独立超时与重试次数；指数退避；4xx（除 429）不重试；**空产出检测**。

### 核心决策：确定性注入 vs Tool Use（函数调用）

这是本项目最重要的架构决策。替代方案是给模型 `search_memory` 工具让它自己检索记忆：

- LLM 决定「调不调」是概率行为，不可复现——今天记得、明天忘
- 工具调用引入额外轮次，延迟翻倍
- 失败无法归因：没调？调了没命中？命中没用上？全是黑盒

确定性装配的收益：行为可复现（可做 A/B 评测）、Trace 全程可观测（问题归因到层）、记忆命中质量只取决于检索算法本身。

> 一句话：陪伴场景的记忆必须 100% 可靠——用户对「他忘了我们说过的话」容忍度是零。我把「是否记得」从概率问题变成了工程问题。

### 其他选型对比

- **SSE vs WebSocket**：聊天只需单向推流，SSE 建立在 HTTP 上（Hono 原生 streamSSE、断线自动重连），WebSocket 的双向能力在此场景是纯开销。
- **固定预算 vs 全塞上下文**：Lost in the Middle 问题——LLM 对上下文中部信息利用率显著偏低，无限堆上下文既贵又稀释关键信息；固定预算强制取舍且成本上限可预测。
- **Trace 带版本号**：不同版本装配器的产出可分组对比，M6 benchmark 的实验基础。

### 踩坑经验（来自真实失败案例调研）

个人开发者实测：Claude 遇敏感内容会**静默返回空内容**，导致情感浓度最高的记忆反而最容易被丢。因此 Gateway 把空产出定义为错误并触发重试——这是第一道防线，M3 提取管道的产出校验是第二道。

### 效果

- 每轮请求模型看到的 prompt 由服务端确定性生成，人设永远在场（与窗口分属不同层，不会被挤掉）
- 全部消息服务端落库，Trace 完整记录「模型看到了什么」
- 失败路径真实验证：401 → SSE error 事件 → 用户消息不丢
- 五层占位接口就位，M3-M6 每个里程碑 = 实现一层 + 接入装配器

---

## M3 记忆提取管道：Turn 级提取 + 章节蒸馏 + Write Gate

### 做了什么

记忆从「存对话原文」进化为「存结构化记忆」。三个组件：

**1. Turn 级提取**（`server/src/services/extraction.ts`）
每轮聊天结束后异步触发（不阻塞聊天流）：把这轮 user+assistant 交换交给提取模型（温度 0.2），产出结构化 JSON，写入 `memories` 表。四类记忆：

| 类型 | 内容 | 示例 |
|---|---|---|
| fact | 用户稳定事实/偏好 | 「她怕黑，睡觉要开小夜灯」 |
| episode | 具体事件（带原文回链 sourceMessageIds） | 「周六晚上一起看了流星雨」 |
| emotion | 情绪状态及原因 | 「她因为项目延期很焦虑」 |
| event | 关系事件 | 「约定下个月一起过纪念日」 |

**2. Chapter Distillation**（`server/src/services/distillation.ts`）
每累计 24 条消息（约 12 轮）触发：把这章原文蒸馏成 200-400 字章节摘要写入 `chapters` 表，经装配器注入 L4 摘要层（最近 3 章）。`messageIds` 保留回链。

**3. Write Gate 分级写入**（human-in-the-loop）
- importance ≥ 0.6 且 confidence ≥ 0.7 → 自动写入（active）
- 其余 → `pending_review`，通过 `PATCH /api/memories/:id` 批准（approve）/ 拒绝（reject）/ 编辑（edit）

### 防坑三件套（来自真实失败案例，面试的核心素材）

| 坑（来源） | 防线 |
|---|---|
| 压缩过度：一晚上的事被压成 14 个字，情感细节全丢（个人开发者实测） | episode 字数下限 15 字，不足拒绝；蒸馏摘要 < 60 字视为失败放弃本次；**所有记忆带 sourceMessageIds 原文回链**——摘要丢了也能取回原文 |
| 敏感内容静默跳过：Claude 返回空导致最重要的记忆被丢（个人开发者实测） | Gateway 空产出抛错（M2 第一道）+ 提取结果回写 assistant 消息 meta（M3 第二道）——失败可见、可追溯，绝不静默丢失 |
| JSON 产出被 markdown 包裹/污染 | 解析前剥 fence、截取 [ ] 区间；不合法换更严格提示重试一次 |

### Temporal Supersession（时态失效）

新旧事实矛盾时**不删旧记录**：旧记录 `status='superseded' + validTo=now`，新记录正常写入。关系是会演进的（「喜欢喝拿铁」→「最近改喝美式了」），删除会让「他为什么记得我以前爱喝拿铁」这种历史感消失。M3 用字符二元组 Jaccard ≥ 0.45 做轻量冲突检测，M4 换 embedding 余弦相似度。

### 为什么 24 条一章

- < 20：蒸馏频繁、成本高、摘要碎片化丢叙事连续性
- \> 40：近期窗口（24 条）滑出后到下次蒸馏之间存在「遗忘空窗」
- 24 是窗口大小 = 无缝衔接：原文滑出窗口的同时恰好进入章节摘要

### 提取为什么异步、为什么放服务端

- **异步**（`void runExtraction(...)` 不 await）：提取增加 1-3 秒延迟，用户不该等；聊天流走 SSE 结束后触发
- **服务端**：和 M2 同理——提取是记忆系统的职责，不该依赖前端是否开着页面；提取用的 LLM 凭证由本轮请求 Header 透传，不落库

### 效果（冒烟验证）

- 假 key 提取失败 → 错误完整记入 assistant 消息 meta（`{error: "LLM API 401..."}`），主流程零影响
- Write Gate 全链路：pending_review 列表 → approve 变 active → reject 变 rejected
- 一轮真实对话完成后（配好 key）：`GET /api/memories` 能看到提取出的记忆，聊满 12 轮后 L4 摘要层开始注入

---

## M4 混合检索：向量 + 全文 + 时序，RRF 融合

### 做了什么

让 L5 Retrieved 层真正具备「从成百上千条记忆里召回真正相关的那几条」的能力。三路召回 + 融合：

```
                  用户当前输入
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   通道 A 向量       通道 B 全文       （无第三通道——
   pgvector <=>       tsvector @@        时间衰减 & importance
   余弦相似度        websearch_to_tsquery  作为融合权重参与）
        │              │
        └──────┬───────┘
               ▼
          RRF 融合 (Cormack 2009, k=60)
               ▼
     score × exp(-age/half_life) × (0.5 + 0.5×importance)
               ▼
         top 8 注入 L5 Retrieved 层
```

### 三路召回 vs 单一检索的选型对比

| 通道 | 擅长 | 不擅长 | 实现 |
|---|---|---|---|
| **向量**（pgvector HNSW） | 同义改写（「开夜灯」↔「怕黑睡不好」）、语义关联 | 专有名词、数字、纪念日日期 | `embedding <=> query_vec` 余弦 |
| **全文**（PostgreSQL tsvector + websearch_to_tsquery） | 字面命中（「三周年」「纪念日」）、低延迟（不需要 embedding API） | 改写、抽象表达 | `ts_rank_cd(content_tsv, query)` |
| **重要性 + 时间** | 让旧/不重要记忆自然下沉 | 不能独立召回 | 融合权重 `importance × exp(-age/30天)` |

**为什么必须混合**：纯向量会让"周年纪念日"这种数字串被淹没（embedding 把 "2024-03-14" 和普通文本混在一起）；纯全文会让"开夜灯"和"怕黑"永远凑不到一起——互补关系。

**为什么是 RRF 不是加权打分**：RRF 只用「排名」做融合，不需要对向量分数和 BM25 分数做归一化（归一化是工程黑洞，每换一次向量就要重调权重）。RRF 公式 `Σ 1/(k+rank_i)` 把多路排名降序相加，对每路单独不敏感，工程鲁棒性显著更好。

### 时序衰减设计

记忆不是越久越值钱，但也不是「一刀切按时间删」。半衰期 30 天 + importance 调节：

- `weight = rrf_score × exp(-age_days / 30) × (0.5 + 0.5 × importance)`
- importance=1.0 的记忆：3 个月后权重降到 25%，但仍能召回
- importance=0.3 的寒暄：3 个月后权重降到 5%，自然被排到后位
- 不直接删除——「他为什么记得我们刚开始聊过什么」这种历史感对情感陪伴很重要

### 工程实现细节

**1. Schema 层**（`server/src/db/schema.ts`）

```ts
// pgvector customType —— 让 Drizzle 能读写 vector(1536) 列
const embedding1536 = customType<{ data: number[]; driverData: string }>({
  dataType() { return 'vector(1536)' },
  toDriver(v) { return `[${v.join(',')}]` },
  fromDriver(s) { return s.slice(1, -1).split(',').map(Number) },
})
```

**2. SQL migration**（`server/drizzle/0002_hybrid_retrieval.sql`）

- `CREATE EXTENSION vector`（Supabase 默认开启）
- `embedding vector(1536)` 列 + HNSW 索引 `WITH (m=16, ef_construction=64)`
- `content_tsv tsvector GENERATED ALWAYS AS (to_tsvector('simple', content)) STORED` —— 生成列自动维护，无需应用层维护
- `GIN (content_tsv)` 全文索引

**为什么 HNSW 不要 IVFFlat**：IVFFlat 需要训练（先有数据再建索引），小数据集或新表会卡；HNSW 无训练即用，对 1k-100k 量级的记忆集合召回质量更好。

**为什么 tsvector 用生成列不用触发器**：生成列由 PG 自身维护，不可能漏触发、不可能不一致；触发器一旦应用逻辑挂了 tsvector 就脏了。

**3. Embedding 服务**（`server/src/services/embeddings.ts`）

- OpenAI 兼容协议 → 任何 OpenAI/Azure/SiliconFlow/智谱 endpoint 都能用
- 配置由前端 env 注入（`EMBEDDING_API_KEY` / `EMBEDDING_BASE_URL` / `EMBEDDING_MODEL`）—— 用户数据主权
- 单条 + 批量；超时 + 重试；空产出检测
- **优雅降级**：无 key 时不崩溃，retrieval 跳过向量通道只走全文

**4. 检索入口**（`server/src/services/retrieval.ts`）

`searchMemories(query, conversationId, topK=8)` 单一入口；每次返回 `{ items, trace }`，trace 含「向量通道命中数 / 全文通道命中数 / 总耗时 / embedding 是否可用」——为 M6 benchmark 铺路。

**5. 检索上下文装配**（`server/src/services/context-assembler.ts`）

L5 层从 null 占位升级为真实注入：

```
[fact] 她怕黑，睡觉要开小夜灯
[episode] 第一次见面是去年春天樱花季
[event] 约定下个月 14 号一起过纪念日
```

**trace 版本号 bump** `ctx-assembler/0.2 → 0.3`：M4 之后装配器产出形态变了，trace 用于 M6 不同版本装配器的对比评测。

**6. Debug 端点**

`GET /api/retrieval/search?conversationId=...&q=<query>&topK=8` —— 不依赖前端也能 curl 验证检索质量。

### 与 M3 的协同：Temporal Supersession 升级

M3 用字符二元组 Jaccard 检测事实冲突（无 embedding 的过渡方案）。M4 落地后，supersede 可以升级为 embedding 余弦相似度，准确性更高——但作为**可选优化**，M3 的字符方案在没有 embedding key 的降级路径里依然工作。

### 踩坑记录

| 坑 | 修复 |
|---|---|
| Drizzle 没有内置 vector 类型 | customType 适配，toDriver/fromDriver 处理 `[1,2,3]` 字符串格式 |
| `applyFinalRanking` 想 await 但用 `.then()` 返回 Promise 没 await | 类型系统兜住了，但运行时是 Promise；改回 `async/await` |
| 检索 trace 包含 embedding 状态 | 让前端能在零 embedding 配置时也确认「检索降级到了纯全文」 |

### 效果

- L5 Retrieved 层从「永远为空」变为「真正注入相关记忆」
- Context Trace 多了 `vector=Nhit, text=Nhit, duration=NNms` 的可观测数据
- 用户即使不配 embedding key，**纯全文通道也能工作**——渐进式启用
- 三路召回 + RRF 融合 + 时序衰减 + importance 加权，构成本项目最复杂的检索管线

### 一句话定位

> 我用确定性装配解决了"是否记得"；用混合检索解决了"记得多准"。两个里程碑合起来，把"他忘了我们说过的话"从产品缺陷变成了工程可控变量。

---

## M5 情感特化层：关系状态机 + 情绪坐标 + 遗忘曲线

### 为什么要做情感层

M1-M4 解决了"是不是能记起来"（装配 + 检索），但情感陪伴产品的灵魂是**关系的温度**：用户说"我今天很难过"，需要 AI 知道——你们现在处于哪个阶段（亲密期还是冲突中？）、今天发生了什么、之前类似时刻 ta 怎么回应过。M5 把"关系"从隐变量变成显式的三维坐标，让 AI 的回应有"历史感"。

### 三个核心组件

#### 1. 关系状态机 `services/relationship.ts`

把"亲密/信任/冲突"三个连续值（0-1）和"关系阶段"（initial/warming/intimate/conflicted/distant）做成可计算、可追踪的状态：

```
intimacy   亲密度  0-1   你们多亲密
trust      信任度  0-1   你信不信任 ta
conflict   冲突度  0-1   最近摩擦多少
phase      阶段    自动从三维推出
```

**阶段判定规则（可读、可解释、不靠黑盒 LLM）**：
```
conflict ≥ 0.5                               → 'conflicted'
intimacy ≥ 0.7 ∧ trust ≥ 0.6 ∧ conflict < 0.3 → 'intimate'
intimacy < 0.3 ∧ trust < 0.5 ∧ conflict ≥ 0.3 → 'distant'
intimacy ≥ 0.3                                → 'warming'
其余                                          → 'initial'
```

**状态推进来源**：`emotional_events` 表的事件流累计 delta。事件由 M3 的提取管道触发——每条触动关系维度的记忆自动投影成情感事件。

**关键设计——节流**：同一 dimension 在窗口内累计 delta 软上限（默认 5 分钟、阈值 0.15），防 LLM 反复触发同类事件导致三维剧烈波动；超出按比例缩放——**事件本身保留记录，只缩影响力**（可追溯 vs 简单丢弃）。

**乐观锁 version 字段**：并发提取时不会互相覆盖（UPDATE WHERE version=old）。

#### 2. memories 加情感四件套

```
valence              效价 -1~1   这条记忆的情感色彩
arousal              唤醒度 0-1  激动程度
emotional_intensity  强度 0-1    影响遗忘曲线（默认 0.5）
emotional_dimension  维度        intimacy|trust|conflict|arousal|null
```

LLM 提取时多输出四个字段（prompt 里加几行指导，不增加新结构）。**`emotional_dimension` 是从「普通记忆」到「情感事件」的开关**——填了才会触发状态机，不填就是普通记忆。

#### 3. 服务端自动投影 → emotional_events

不让 LLM 输出第二份结构。规则（`extraction.ts/projectToEmotionalEvents`）：

```
emotional_intensity ≥ 0.5 且 emotional_dimension 非空 → 投影
intensity 0.7 → 影响量 0.07，1.0 → 0.10（单次最大）
valence ≥ 0 → 正向事件；< 0 → 负向事件
dimension 决定 delta 应用到哪个维度：
  intimacy + 正 → +0.07 intimacy
  intimacy + 负 → -0.07 intimacy
  conflict + 负 → +0.07 conflict（真实冲突）
  trust   + 负 → -0.07 trust
  arousal       → 仅记录，不改三维（情感色彩不改变关系结构）
```

`trigger_kind` 服务端推断：`argument`/`intimate`/`promise`/`milestone`/`casual`——M7 前端可按类型筛选事件流。

### 检索层升级：M4 指数衰减 → M5 艾宾浩斯遗忘曲线

M4 公式：`score = rrf × exp(-age/30) × importance`

M5 公式：
```
S = base × (1 + 0.5 × importance) × (1 + 0.5 × emotional_intensity) × (1 + 0.3 × ln(access+1))
strength = exp(-age / S)
score = rrf × strength
```

**为什么是艾宾浩斯**：
- M4 的 `exp(-age/30)` 对所有记忆一视同仁——但用户记一辈子的事（比如第一次表白）和昨天点过赞的帖子，衰减速度不该一样
- M5 让 **重要 / 情感强 / 被反复回忆** 的记忆有更高的稳定度 S，衰减更慢
- 这就是「复习效应」（被访问过的更难忘）+「情感强化」（强度高的更不忘）

**实测对照（默认 base=30 天）**：

| 记忆类型 | S（稳定度） | 90 天后强度 | 一年后强度 |
|---|---|---|---|
| 普通寒暄 fact | ~47 天 | 0.15 | 几乎归零 |
| 表白 episode（intensity 0.9） | ~80 天 | 0.33 | 0.01 |
| 反复回忆过的亲密记忆 | ~120 天 | 0.47 | 0.05 |

→ 日常记忆自然遗忘，关系大事被长期保留。

### 装配器升级：L1/L2 从占位到实装

**L1 Relationship 注入示例**：
```
【你们的关系现状】
当前阶段：亲密期
亲密度 0.78 ｜ 信任度 0.82 ｜ 冲突度 0.15
最近的关系变化：
- [intimate] 亲密度+0.08
- [promise] 信任度+0.06
- [casual] 亲密度+0.02
```

**L2 Ongoing 注入示例**：
```
【最近在发生的事】
本会话已累计 48 条消息，继续当前话题。
（或：上一章摘要的前 5 行）
```

trace 版本 bump 到 `ctx-assembler/0.4`（评测对比用）。

### 三个值得讲的工程决策

1. **「让 LLM 多输出 4 个字段，但不输出新结构」**
   反面教材：很多团队会让 LLM 同时输出「memories」和「relationship_events」两份独立的输出，prompt 复杂度爆炸、schema 容易冲突、模型混淆。我让 LLM 在每条记忆上多填四个字段（intensity/valence/arousal/dimension），服务端按规则**投影**成事件流——LLM 的工作量增加 5%，工程可控性 100%。

2. **「影响量由服务端算，不让 LLM 输出」**
   反面教材：让 LLM 输出 `delta: { intimacy: +0.07 }` 等数值字段，LLM 容易输出任意夸张的数字（+0.5、+1.0）导致状态机暴走。我用 intensity 字段 + 服务端映射公式 `magnitude = intensity × 0.1` —— 0.7 → 0.07，1.0 → 0.10，单次最大 0.1。再加节流，**LLM 的输出永远在可控范围内**。

3. **「艾宾浩斯曲线比指数衰减更接近人脑」**
   面试时可以说：「M4 用 exp(-age/30) 是工程简化版——所有记忆衰减一样快。M5 升级为艾宾浩斯，加入「重要性」「情感强度」「访问次数」三个稳定度因子。日常寒暄 90 天后自然遗忘，但表白/纪念日类记忆一年后仍能被检索到——这跟人脑规律一致。」

### 一句话定位

> 我用 M5 把"关系"从隐变量变成显式的三维坐标 + 可追踪的事件流 + 艾宾浩斯遗忘曲线，让 AI 的回应有温度、有历史感。这是从「记忆系统」升级到「情感陪伴系统」的关键一步。

---

## M6 可观测 + Eval：Context Trace 面板 + 小型 Benchmark

### 为什么要做这一步

M1-M5 都是在"做东西"——加 schema、加服务、加算法。但没有评测就无法回答：
- 当前检索到底召回得好不好？
- 新加的优化是真有效还是噪音？
- 哪些 query 类型是当前系统的薄弱点？

M6 = 把"凭感觉"变成"看数据"。

### 做了什么

#### 1. 自建检索 Benchmark —— `server/benchmark/`

- **评测集 `queries.ts`**：28 条 query 覆盖五类意图
  - keyword (5)：纯关键词精确召回
  - semantic (7)：同义改写、场景泛化
  - emotion (6)：抽象情感/关系定义
  - cross (5)：跨条推理
  - negative (5)：负样本（不该召回的）
- 每条 query 标注**期望命中的记忆 ID 列表 + 期望排序 + 评测者备注**
- 基于现有会话沉淀的 8 条记忆构建评测集，未来新对话/新记忆可增量更新

- **评估脚本 `run-benchmark.ts`**：
  - 调 `/api/retrieval/search` (topK=8) 跑 28 条 query
  - 计算 **Recall@K / Precision@K / NDCG@K / MRR** 四类指标
  - 按分类目聚合 + 输出每条 query 的命中详情
  - 输出 `benchmark/results.json` + `benchmark/last-run.log` 人类可读

- **分析报告 `analysis.md`**：首跑结果 + 问题归因 + 优化建议（按收益/成本排序）

#### 2. 首跑结果（n=28 queries）

| 指标 | 数值 | 评价 |
|---|---|---|
| Recall@8 | **100%** | 满分——所有期望记忆都被召回 |
| Precision@8 | 25.4% | 受数据集规模限制（仅 8 条记忆，Top-8 装满天然 P 低） |
| NDCG@8 | 80.8% | 良好，有提升空间 |
| MRR | 82.1% | 良好，首个真实命中平均在前 1.2 位 |

**分目指标**：

| 类别 | n | R@8 | P@8 | NDCG | MRR |
|---|---|---|---|---|---|
| keyword | 5 | 100% | 25% | **98%** | **100%** |
| semantic | 7 | 100% | 27% | 77% | 86% |
| emotion | 6 | 100% | 36% | 68% | **72%** ⚠️ |
| cross | 5 | 100% | 27% | 73% | 68% |
| negative | 5 | 100% | 10% | 85% | 85% |

#### 3. 关键问题发现

**问题 1：emotion/cross 类 MRR 偏低** —— 抽象 query 时，重要但无关的记忆排前

- 例：query"我好像只在你面前才放松"，期望前 3：温柔眷恋 / 跨越很久 / 超出职业关心
- 实际 Top-3：焦虑手术 / 企鹅担心南极 / 深夜医院
- **根因**：importance + emotional_intensity 加权让"高重要性但语义无关"的记忆抢前

**问题 2：efa487bc（深夜医院）总抢第一** —— 同一会话 28 条 query 中，它在 80% 的 query 里都进 Top-3

**根因**：
- 长度长 + importance=0.7 + emotional_intensity=0.5 + 时间新 → 艾宾浩斯 S ≈ 50 天
- 公式：`S = 30 × (1+0.35) × (1+0.25) × 1 = 50` 天的稳定度
- 即便 query 语义无关，衰减后强度仍然有 0.86（今天看）

#### 4. 优化建议（按收益/成本排序）

| 优化 | 收益 | 成本 | 建议 |
|---|---|---|---|
| importance 系数 0.5 → 0.2 | emotion/cross NDCG +10~15% | 0 | 当前数据集太小，需更多数据再验证 |
| Query 改写 / HyDE | emotion MRR 0.72→0.85+ | 每检索 +1 LLM 调用 | 50+ 条记忆再做 |
| Cross-encoder rerank | NDCG +5~10% | 每检索 +1 cross-encoder | 1k+ 条记忆再做 |
| 调整 importance 数值 | 局部 | 低 | 不推荐——手工调不是可持续方案 |

> **决策**：当前数据集只有 8 条，rerank/HyDE 的收益会被噪音淹没，**暂不做**；等数据量到 50+ 条再迭代。

#### 5. Context Trace 面板前端 —— `src/views/TraceView.vue`

**目的**：每轮 LLM 看到的 Context 七层到底注入了什么，可视化审计

**功能**：
- 顶部：会话总览（trace 轮数 / 平均字符 / 平均耗时 / L5 命中率 / 向量可用率）
- 列表：每条 user/assistant 配对，摘要行展示注入层 emoji + 字符数 + 总耗时
- 展开后：
  - **七层注入明细**：每层是否注入 / 字符数 / 来源描述（带颜色区分）
  - **L5 检索明细**：每条命中记忆的 kind / source（向量/全文/双通道）/ importance / 实际 score / 内容
  - **LLM 调用元数据**：model / retries / 耗时
  - **错误信息**：本轮若有 API 错误会显示
  - **提取元信息**：本轮提取了多少记忆 / supersede / pending review

**导航**：导航栏 → Trace，路由 `/trace/:conversationId?`

**纯前端**：直接读 `GET /api/conversations/:id/messages`，**不依赖新后端接口**

#### 6. 后端扩展 —— `server/src/services/context-assembler.ts`

为了让 Trace 面板能看到 L5 检索命中明细，给 ContextTrace 加两个字段：

```ts
export interface ContextTrace {
  // ...原有字段
  retrievedItems?: Array<{          // 新增
    id: string
    kind: 'fact' | 'episode' | 'emotion' | 'event'
    content: string
    importance: number
    score: number
    source: 'vector' | 'text' | 'both'
  }>
  retrievalStats?: {                // 新增
    hits: number
    durationMs: number
    vectorEnabled: boolean
  }
}
```

装配器版本 `ctx-assembler/0.4 → 0.5` —— 用于 M6+ 不同版本装配器的对比评测

### 三个值得讲的工程决策

#### 1. **「自己写 benchmark，不依赖第三方评测集」**

反面教材：很多团队会用 BEIR / MS-MARCO 等公开数据集。但那些数据集是英文 / 通用领域 / 文档级，与本项目"中文 / 情感陪伴 / 长记忆"的场景差异巨大。公开数据集的 SOTA 模型到了我们的场景可能掉 30%。

自建 28 条 query 的成本是 1 小时工作，但所有 query 都是真实场景：
- 用户在产品里会问什么？
- "你为什么对我这么好" / "我们认识多久了"——公开评测集不会有这种 query

#### 2. **「评测集要包含负样本」**

正面例子只能证明"能召回对的"，但不能证明"会不会召回错的"。

评测集里专门加了 `neg-weather` / `neg-cat` —— "今天天气怎么样""我家猫生病了"，期望 0 命中。这测试的是 Precision 上界：

- 如果返回 Top-8 全是噪音 → 系统错把噪音当真相关（rerank 该上场了）
- 如果返回 0 条 → 系统完全失忆（这种情况反而允许——AI 应该说"我没听你提过这个"）

#### 3. **「Trace 面板不是新接口，是 messages.meta 的可视化」**

M2 装配器在 assistant 消息 meta.trace 里已经写了"每层字符数 / 总耗时 / 版本号"。M6 没增加新接口，纯前端组件读 messages 自己渲染——

- **零后端改动**：部署不影响
- **历史可追溯**：跑过的每轮对话都能展开看（即便装配器已经迭代到 0.5，老的 0.2 trace 还能看）
- **可视化即文档**：看到 trace 就理解了 M2-M5 的设计

### 效果

| 维度 | M5 之前 | M6 之后 |
|---|---|---|
| 检索好坏 | 凭心情判断 | **Recall@8 = 100%, MRR = 0.82** |
| 检索优化 | 改完不知道有没有效 | **改完跑 benchmark，对比 results.json** |
| 七层注入 | 黑盒 | **每轮可视化** |
| 异常定位 | 看日志 | **Trace 面板 + LLM error 红色高亮** |

### 一句话定位

> M6 把这个项目从"我觉得好用"变成"我有数据"。从这一里程碑开始，**所有优化都要先跑 benchmark 看 NDCG 是否提升**，凭感觉调参的时代过去了。

---

## M7 前端整合：记忆面板 + 数据导出

### 做了什么

| 模块 | 内容 |
|---|---|
| **记忆面板** `MemoryPanelView.vue` | 浏览所有 AI 自动提取的记忆（M3 四类 fact/episode/emotion/event + M5 情感维度），按会话/类型/排序过滤，4 个 Tab（待审/已激活/已取代/已拒绝），approve/reject/edit/delete 操作 |
| **数据导出** `server/src/routes/export.ts` | 单会话导出 JSON 完整快照（conv + messages + memories + chapters + relationship）+ Markdown 人类可读叙事（章节摘要 → 完整对话 → 提取的记忆） |
| **MemoryLibraryStore** `src/stores/memoryLibrary.ts` | 新 store，与旧 memories.ts（用户手动回忆）区分；调后端 /api/memories，封装 approve/reject/edit/remove + 派生（pending/active/superseded/rejected） |
| **API client 扩展** `src/utils/backend.ts` | 加 listMemories/updateMemory/deleteMemory/exportConversation；DTO 包含 M5 情感四件套（valence/arousal/intensity/dimension） |
| **路由 + 导航** `router/index.ts` + `Navbar.vue` | 加 `/memories` 路由 + 导航栏加"🧠 记忆库" |

### 选型对比

| 决策 | 选择 | 理由 | 弃选方案的问题 |
|---|---|---|---|
| 旧 memories.ts 是否改 | **保留，新建 memoryLibrary** | TimelineView 还在用，强制迁移会破坏"用户手动写回忆"产品定位 | 合并：用户手动回忆和 AI 提取语义不同，UI 表现也不同 |
| Markdown 章节排序 | **按 chapterIndex 升序 → 完整对话 → 记忆** | 先摘要 → 后详情，符合阅读节奏 | 倒序：摘要失去指引作用 |
| JSON 排除 embedding | **不返回 1536 维向量** | 减少 payload 体积 ~6KB/条；客户端不需要 | 完整返回：导出文件会膨胀 |
| Markdown 转义 | **仅转义 ` \| \` \* \_ \#`** | 防表格/标题被干扰，不破坏中文 | 全转义：破坏可读性 |
| 文件名 | **`会话标题.md`** | 用户友好，可直接看懂是哪个会话 | UUID：不可读 |
| 软删除 vs 物理删除 | **reject 软 + delete 物理** | reject 留审计，delete 永久 | 统一物理：删错无法恢复 |
| Tab 切换而非分页 | **4 个 Tab** | 8 条记忆量级无需分页 | 分页：增加导航成本 |
| 编辑走 inline 弹窗 | **弹窗 + textarea** | 长内容编辑需要大空间 | 行内编辑：难编辑超过 200 字内容 |

### 亮点

**1. 三层信息架构**（一眼看清）
- 头部状态栏：总数 + 待审数
- Tab 切换：4 个状态互斥
- 单条记忆卡：图标 + 类型 + 状态 + 情感维度 + 重要度/置信度/情感强度/效价 + 原文 + 操作

**2. Write Gate 操作完备**
- 待审状态显示「批准 / 拒绝」
- 任意状态可「编辑 / 删除」
- 操作即时更新列表（无需重新加载）

**3. 导出格式双轨**
- JSON：程序可读，含完整字段（含 relationship 状态），备份/迁移用
- Markdown：人类可读，含章节叙事，朋友圈分享/日记打印用

**4. Markdown 的「摘要 → 对话 → 记忆」三段式**
- 不只是单纯 dump，而是叙事结构
- 用户分享时不需要解释这是什么

### 踩坑

| 坑 | 原因 | 解决 |
|---|---|---|
| 旧 memories store 命名冲突 | 已经叫 `memories` 了 | 新 store 改名 `memoryLibrary`，文件也独立 |
| export JSON 含 1536 维 embedding 数组 | 文件膨胀 ~6KB/条 | SQL 层 select 不带 embedding 列 |

### 效果

| 维度 | 之前 | M7 之后 |
|---|---|---|
| 记忆可视化 | 后端 SQL 直查 | **前端 UI 浏览 + 审批** |
| 数据可移植性 | 数据库绑死 | **JSON 导出 = 跨系统迁移** |
| 分享回忆 | 不可能 | **Markdown 导出 = 朋友圈分享** |
| Write Gate | 后端接口存在但无人用 | **前端面板点亮完整工作流** |

### 一句话定位

> M7 把项目从「能跑」变成「能用」。记忆面板让 M3 提取的所有价值真正落到用户眼前，导出让数据不再被锁死在数据库里。

---

## M8 评测真实化：合成数据集 + 全库检索 Benchmark

### 为什么要做这一步

M6 的 benchmark 首跑 Recall@8=100% 看起来完美，但这是**数学假象**：库里只有 8 条记忆，Top-8 恰好装满——任何策略都能"全召回"。评测要有区分度，必须先有足够大、足够难的数据集。

### 做了什么

| 模块 | 内容 |
|---|---|
| **合成数据集** `benchmark/seed-data.ts` | 20 个场景、约 111 条记忆，覆盖人物设定 / 情感事件 / 演化链 / 关系事件四类，每条带 importance / 情感强度 / 时间戳（真实分布） |
| **Hard Negative 设计** | 数据集的难点刻在记忆之间：她喜欢茉莉花 vs 她花粉过敏（送花类 query 的双答案）、求职焦虑→offer→入职的演化链（temporal supersession 考点）、吵架→和好（关系状态断层考点） |
| **灌库脚本** `scripts/seed-conversations.ts` | 场景 → 会话 → 消息 → 记忆四层灌入，embedding 走真实 API |
| **Query 集扩容** `benchmark/queries.ts` | 8 → 61 条，七类：keyword / semantic / emotion / cross / temporal / **negative** / **hardneg**；期望 ID 由脚本从 summary 匹配生成（`_gen-ids.py` + `memory-ids.ts`），避免手写 UUID |
| **全库检索** `retrieval.ts` | `conversationId` 从必填改可选，benchmark 跨全部 20 个会话检索——这才符合"新 Session 想起旧记忆"的真实产品场景 |

### Benchmark v2 真实基线（61 queries，111 条记忆）

| 类别 | R@8 | 解读 |
|---|---|---|
| keyword | 93% | 字面匹配强，符合预期 |
| semantic | **15.5%** | 几乎全灭 |
| hardneg | **10.4%** | 全灭——最难类别 |
| **Overall** | **45.9%** | 之前 100% 的泡沫被戳破 |

### 关键问题发现（比分数更重要）

诊断脚本 `_diag.ts` 定位到根因：**向量通道召回正常，排序阶段被遗忘曲线压垮**。

典型病例：query「送她什么花比较好」→ 茉莉花记忆在向量通道排第 1，但最终 Top-8 一条都没有——被 8 条 importance 0.75-0.95 但语义无关的记忆（极光 / 签合同 / 橘子）挤掉。

### 一句话定位

> M8 把评测从"自我安慰"变成"照妖镜"：合成数据集带 hard negative，一次跑分暴露了藏在排序公式里的系统性缺陷。

---

## M9 Ranking 修复：遗忘曲线让位于语义相关性

### 核心洞察：两把尺子量纲不匹配

旧公式 `finalScore = rrfScore × decay`，两个因子的量纲差异：

- **RRF 分数区间极窄**：k=60 时 rank-1 ≈ 0.0164，rank-8 ≈ 0.0147，仅差 **11.4%**
- **艾宾浩斯 decay 跨度极大**：0.3 ~ 0.95，约 **3 倍**

窄区间 × 大跨度 = decay 完全主导排序。遗忘曲线本该是"锦上添花的微调"，实际上成了"一票否决的暴君"。

### 修复方案

```ts
finalScore = rrfScore × (DECAY_FLOOR + DECAY_WEIGHT × decay)
// DECAY_FLOOR = 0.90, DECAY_WEIGHT = 0.10
```

decay 从"乘数"降级为"±10% 调节系数"。**推导依据**：摆幅 `(F+W)/F = 1.11` 必须严格小于 rank-1/rank-8 的 RRF 之比 1.114，才能数学上保证语义排名第 1 的记忆永远不被遗忘曲线反转。

### 离线验证（工程严谨性的体现）

网络中断（详见踩坑）期间写了 `scripts/_verify-ranking.ts`：从 DB 取真实 111 条记忆的 decay 分布，蒙特卡洛 5000 次模拟"rank-1 相关记忆 + 7 条高 decay 干扰记忆"：

| 公式 | rank-1 跌出 Top-8 的概率 |
|---|---|
| 旧 `× decay` | **48.3%** —— 与 benchmark v2 的 45.9% 互为印证 |
| 新 `× (0.90 + 0.10·decay)` | **0%** |

### Benchmark v3 结果（61 queries）

| 类别 | v2 | v3 | Δ |
|---|---|---|---|
| **Overall R@8** | 45.9% | **60.0%** | **+14.1pt** |
| hardneg | 10.4% | 58.3% | **+47.9pt** |
| semantic | 15.5% | 40.0% | +24.5pt |
| keyword | 93% | 96.7% | +3.7pt |
| temporal / emotion / cross / negative | — | 68% / 48% / 42% / 70% | — |

诊断 query 复测：「送她什么花」→ 茉莉花排第 1、花粉过敏排第 2/3（v2 时 Top-8 全无关）。

### 配套工程修复

| 修复 | 内容 |
|---|---|
| **DB 直连降级** `db/client.ts` | Supabase pooler(6543) 频繁断连 → `DB_CONN_MODE=direct` 支持直连(5432) |
| **Embedding 磁盘缓存** `embeddings.ts` | `.cache/embeddings.json` 按 model+text 缓存向量，benchmark 重跑不再重复烧额度 |
| **429 退避重试** | 429 用 8s×(n+1) 退避；4xx 非限流错误不重试 |
| **递归 bug 修复** | 缓存层引入的 `embedBatch→embed→embedBatch` 无限递归（RangeError）→ 统一走 `embedBatchFresh` |

### 踩坑：本地代理的"网络中断"骗局

排障一天"外网全断"（HTTP=000），真相是**终端环境变量挂着本地代理 127.0.0.1:7897，代理进程活着但上游节点故障**——CONNECT 隧道建立、TLS 握手失败，curl 全部超时。教训：`HTTP=000 ≠ 没网`，先 `env | grep -i proxy`。此坑还污染了早期的"429 限流"诊断。

### 一句话定位

> M9 是整个评测闭环的价值兑现：用数学推导定位排序公式的量纲缺陷，用蒙特卡洛离线验证，再用 benchmark 量化确认——一次修复换来 hardneg 召回翻 5 倍。

---

## M10 记忆再巩固：让记忆"越用越牢"的自进化层

### 设计思想：借鉴神经科学的 Reconsolidation

神经科学中的记忆再巩固（reconsolidation）：记忆每次被**提取**时会进入不稳定状态，随后被重新巩固——经常提取的记忆更牢固，久未提取的记忆自然淡化。这是"用进废退"的生物学基础，也是 AI 陪伴产品最自然的自进化机制：**用户越常聊到的记忆，越不该被遗忘**。

### 做了什么

`server/src/services/reconsolidation.ts` —— `applyReconsolidation()`：每次记忆被检索命中（提取），异步强化其 `emotionalIntensity` / `importance`：

| 机制 | 实现 | 目的 |
|---|---|---|
| **边际递减** | 强化量随当前值趋近上限而衰减 | 记忆强度有天花板，不会无限膨胀 |
| **高频抑制** | 每天访问 20 次的强化 < 每周访问 20 次 | 防"刷记忆"——重复提取不应线性刷强度 |
| **久别重逢加成** | 距上次访问越久，单次强化越大 | 符合"久别重逢更感动"的直觉 |
| **硬上限** | clamp 至 ceiling | 修脏数据（-0.5 未夹紧的 bug） |
| **低情感记忆豁免** | intensity 低于阈值不参与强化 | 纯事实类记忆无需情感强化 |

### 工程接入：fire-and-forget

在 `context-assembler.ts` L5 检索成功后，对每个命中记忆异步调用再巩固——**不 await、不阻塞对话流**。用户聊天零感知，记忆在后台自进化。

### 验证

- 单元测试 `scripts/_test-recon.ts` **12/12 通过**（含每天/每周访问频次对比、上限夹紧、低情感豁免、脏数据修复）
- 典型数据：每天访问 20 次 → 强度 0.697（高频抑制生效）；每周访问 20 次 → 1.000（封顶）

### 与 M9 的协同

再巩固改的是记忆的 `decay` 输入值（通过 lastAccessedAt / intensity），而 M9 已把 decay 在排序中的权重压到 ±10%——**两者是自洽的**：自进化改变的是"什么长期留下来"（写入层面），排序层面则由语义相关性主导。即使某条记忆被强化到顶，也只带来 10% 的排序加成，不会重演 v2 的排序灾难。

### 一句话定位

> M10 给记忆系统装上生物学的"用进废退"：常聊的记忆越用越牢，冷僻的记忆自然淡出——而且强化幅度被 M9 的排序修复严格约束，自进化永远不会失控。

---

## M11 E2E 端到端模拟评测：让模拟用户真的和 AI 聊了 30 轮

### 为什么要做

M8 的 benchmark 只测检索层：记忆是直插 DB 的、query 是手写的。三个盲区始终没被数字覆盖——**提取器实测质量**（M3 从未在真实对话流里验证过）、**端到端记忆引用**（召回了不代表回复会用上）、**记忆张冠李戴**（会不会把 A 的事记成 B 的）。补齐方式只有一种：走完整链路真聊。

### 做了什么

`server/scripts/e2e-sim.ts`，三阶段全真实链路：

1. **Phase 1**：模拟用户脚本走真实 `POST /chat` 和黎深聊 20 轮（DeepSeek 流式回复 → 轮询 `meta.extraction` 等 M3 提取入库 → 下一轮）。剧情全部新编（烘焙课、夜跑演化链、考研、吵架和好……），与 111 条种子记忆零重叠，避免全库检索污染判分。
2. **Phase 2**：10 条探针问题（"我最近在学做什么？""晚上给我喝点什么？"），走真实检索+组装+回复。
3. **Phase 3**：DeepSeek 当 LLM-as-judge，逐条事实判分：回复里是否体现、原文证据、是否编造。

### 结果

| 指标 | 结果 |
|---|---|
| M3 提取实测 | 35 条 / 20 轮，演化链、hard negative 都抓到 |
| 探针事实命中率 | 64.7%（11/17） |
| 探针完全命中 | 6/10 |

表现好的探针很惊艳：夜跑探针五事实全中且带人设延续（"护膝戴了没？"）。

### 重大发现：幻觉固化回路（hallucination feedback loop）

用 DB 时间戳实锤的完整链路：

```
09:21:03  探针回复：黎深把用户说的"司康"幻觉成"肉桂卷"
09:21:07  M3 提取器把这条幻觉当事实写入记忆库
（此后每次检索"烘焙"都拿到假事实，永远洗不掉）
```

滚雪球版本更严重：turn15 黎深随口编"民宿老板养雪豹幼崽" → 提取入库 → 后续探针检索召回 → 继续编 → 再提取强化 → 固化为"用户一直想看真雪豹，曾多次念叨"——**用户从未说过**。20 轮 + 10 探针共产生 ≥5 条假记忆。

这是产品级 bug：LLM 的一句随口发挥，经提取变成永久事实，再经检索反复自我强化——**陪得越久，人设越失真**。直接催生 M12。

### 次级发现

- 跨会话污染：种子数据的"汤圆猫"在探针里被召回（生产 by design，评测算污染）
- 抽象 query 检索弱（B 类问题端到端再现）："我想养宠物" → 怕大型犬记忆仅 rank#8

### 一句话定位

> M11 是评测从"考场"走向"田野"的一步：手写 query 换成真聊 30 轮，换来的是考卷永远考不出来的发现——幻觉固化回路。

---

## M12 幻觉固化回路的斩断：让记忆只信用户亲口说的话

### 问题重述（M11 的发现）

LLM 角色的一句随口发挥（把"司康"说成"肉桂卷"、虚构"雪豹幼崽"），4 秒后就被 M3 提取器当作用户事实写入记忆库；此后每次检索"烘焙"都会召回这条假记忆，角色顺着继续编，再被提取强化——滚雪球直到固化为"用户一直想看真雪豹，曾多次念叨"。**陪得越久，人设越失真，且没有任何机制能把它洗掉。**

根因是一个权限问题：**提取器对"用户说的事实"和"角色编的细节"一视同仁**。

### 方案：origin 标注 + unverified 状态 + 三层闸门

1. **提取层**：每条记忆新增 `origin: 'user' | 'assistant'` 字段。Prompt 给了清晰的判断标准——"这句话的事实内容若去掉 assistant 回复，仅从用户消息能否还原？能→user，不能→assistant"。
2. **写入层**：Write Gate 新规则——assistant 原创的内容**无论多高置信、多高重要，永不自动激活**，落 `unverified` 状态。同时三处权限收紧：
   - 不触发 Temporal Supersession（幻觉无权覆盖真实记忆）
   - 不投影 emotional_events（幻觉不该改变亲密度/信任度）
   - `assistantOriginBlocked` 计数写入 outcome（可观测有没有误伤）
3. **检索层**：`status='active'` 过滤天然排除 unverified → 幻觉进不了 prompt → 回路斩断。

`unverified` 不是垃圾桶：记忆面板新增"角色原创"标签页，用户批准后转 active（human-in-the-loop）——角色说的话用户认了，就是"我们的共同回忆"。

### 为什么不是其他方案

| 备选 | 为什么不选 |
|---|---|
| 提取时直接丢弃 assistant 内容 | 丢掉了角色的承诺/共同回忆（"他说下个月带我看雪豹"），这在陪伴产品里恰恰是核心资产 |
| 检索时给幻觉记忆打"待确认"标注一起注入 | 幻觉仍在上下文里，LLM 会顺着继续编——回路没断，只是贴了个标签 |
| 事后用 judge 清洗 | 治标不治本，每条记忆多一次 LLM 调用，而且洗掉的是症状不是源头 |

选 unverified 隔离的核心逻辑：**在写入端就区分"事实的版权"**——用户是唯一的事实源，角色的发挥只是候选。

### 验证（`scripts/_test-hallucination.ts`）

单元测试（纯函数 `resolveWriteGate`，4/4）覆盖四种边界——其中最关键的一条：`assistant + importance 0.95 + confidence 0.95`（幻觉最危险情形）→ unverified，不是 active。

集成测试复现 M11 的幻觉场景（用户说司康、角色编肉桂卷+雪豹）跑真实链路：实测发现新 prompt 下 DeepSeek 的行为更彻底——**直接不提取角色原创内容**（提取到的 2 条全是用户侧的司康事实），幻觉零泄漏。即使模型某天输出了 origin=assistant，闸门也兜得住（单测覆盖）。

存量数据清理：把 M11 E2E 评测固化的 7 条假记忆（肉桂卷/雪豹/多肉）批量转 unverified——旧的幻觉也能被新机制隔离。

### origin 落库（后续补强）

闸门逻辑生效但 origin 一开始只在提取结果里流转、没有持久化，事后无法审计"这条 unverified 到底是闸门拦的还是实验隔离的"。补强三件套：

1. schema 加列：`memories.origin text CHECK (origin IN ('user','assistant'))`（drizzle/0004 迁移，存量回填 assistant 后再修正）
2. 提取层持久化：`extraction.ts` 的 insert values 带上 `origin: m.origin ?? null`
3. 数据诚实性修正：M11 会话的 38 条 unverified 是实验隔离、真实 origin 未知，回填为 NULL；保留 M12 闸门真实拦截的 8 条 assistant 标注

最终分布：`assistant/unverified=8`（闸门拦截证据）、`null/unverified=45`（实验隔离）、`null/active=134`、`null/pending_review=15`、`null/superseded=8`。

### A/B 对照验证（重跑 E2E）

为确保起跑条件一致：先把 M11 会话全部记忆隔离为 unverified（全库 active 检索不再命中 M11 遗留），再重跑同一套 20 轮 10 探针 E2E（M11 基线备份于 `benchmark/e2e-results-m11-baseline.json`）：

| 指标 | M11（基线） | M12（闸门后） | 变化 |
|---|---|---|---|
| 探针事实命中率 | 64.7% (11/17) | **76.5% (13/17)** | +11.8pp |
| 探针完全命中 | 6/10 | **8/10** | +2 |
| assistant-origin 假记忆自动激活 | ≥5 条 | **0 条** | 闸门生效 |
| 全库 active 含假记忆关键词（肉桂卷/雪豹/多肉） | 有 | **0 条** | 检索层干净 |
| 对话轮次失败 | 1 | 0 | — |

两个未命中的探针与 M11 是同一对，根因与 M12 闸门无关：
- **宠物 0/2**："金毛馒头" imp=0.5 低于 `AUTO_WRITE_IMPORTANCE` 落 pending_review（不进检索）；"怕大型犬"虽 active 但抽象 query 召回排位靠后——都是 M11 已知的 B 类检索问题
- **烘焙 1/3 属裁判误判**：回复实际提到"烤糊过一盘的司康方子"（烤糊事实命中），judge 漏判

### 效果与边界

- 幻觉固化回路：20 轮 10 探针 ≥5 条假记忆 → 0 条 active（A/B 复测确认）
- 代价：角色的主动记忆（未经用户接茬的承诺）需要用户批准才生效——宁慢勿假
- 遗留边界一：如果用户**顺着**角色的幻觉说（"对啊那家雪豹真可爱"），origin 会是 user，幻觉就"洗白"了。这是对话产品的本质局限，只能靠人设层约束角色别乱编
- 遗留边界二（分层定位）：persona LLM **生成层**的即兴编造（三花猫/绿萝/雪豹）每轮仍会出现——但 M12 斩断的是"存储→检索→再强化"的固化回路，生成层幻觉不入库、不自强化，两回事，前者要靠 persona prompt 约束
- 遗留边界三：pending_review 的中等重要度事实（imp 0.5 左右）既没被闸门关注也没进检索，存在盲区——后续可考虑低置信检索兜底或降低探针类事实的写入阈值

### 一句话定位

> M12 给记忆系统立了一条宪法：用户是唯一的事实源。角色的每一句发挥都要先蹲在 unverified 里等用户点头——幻觉从"永久污染"降级为"待审候选"。

---
