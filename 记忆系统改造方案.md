# love-and-secret 记忆系统改造方案

> 定位：从"带人设的流式聊天壳"升级为"有分层记忆、情感状态与可评估稳定性的 AI 情感陪伴产品"。
> 参考来源：小红书个人开发者记忆系统实践（四层存储/混合检索/Ombre Brain 及其 Gateway 二改版）、SillyTavern（World Info/摘要/向量三件套）、Mem0 / Letta / Zep 业界记忆框架。

---

## 1. 现状诊断

| 维度 | 现状 | 问题 |
|---|---|---|
| 架构 | 纯前端 Vue3 + localStorage（`stores/chat.ts`） | 5-10MB 上限、全量重写、无多会话、清空即失忆，**无法承载记忆系统** |
| 上下文 | 滚动 10 条窗口（`ChatView.vue:145-152`） | 第 11 条之前的对话模型完全不可见 |
| 记忆 | 无摘要、无提取、无检索 | 最大空白点 |
| 人设 | 硬编码在 `stores/settings.ts` | 角色卡不是一等公民，无法多角色/多人格演进 |
| 数据模型 | `types/index.ts` 无 Session/Character/Memory 检索结构 | 需重设计 |
| 稳定性 | 流式有错误处理，但无重试/降级/任务路由 | 单一模型承担所有任务 |

**核心矛盾**：情感陪伴产品的命脉是"人格连贯性 × 记忆连贯性"，而模型上下文有限且无状态。现在的项目只解决了"聊起来"，没解决"记得你"。

---

## 2. 调研要点（吸收什么、避开什么）

### 2.1 个人开发者的实战教训（小红书笔记）

1. **四层分层存储 + 向量/关键词混合检索 + 软边界批次切割**（4000+ 对话、三版迭代验证的路线）
2. **最大的坑一：LLM 提取时压缩过度** —— 一晚的事被压成 14 个字，情感细节全丢。教训：摘要必须分层（scene 级细摘要 → 阶段级粗摘要），保留原文引用回链
3. **最大的坑二：敏感内容静默失败** —— Claude 遇敏感 batch 直接跳过，最重要的记忆反而成黑洞。教训：提取任务要有**校验 + 重试 + 多模型路由降级**
4. **检索 benchmark 结论**：向量 + LLM 关联边 + 时序**三路混合**最优
5. **去重**：字符 Jaccard → embedding cosine（语义级去重）
6. **记忆组织**：时间线 → **按主题叙事**（参考 Conway 自我记忆系统：人生阶段 → 一般事件 → 事件具体知识）
7. **Ombre Brain**：本地永久存储、情绪坐标（效价/唤醒度）、遗忘曲线、情感权重 —— "温柔的记忆系统"，情感陪伴场景的差异化核心
8. **Ombre Gateway 二改（关键架构思想）**：**记忆从"被调用"变成"被带入"** —— 不依赖模型自觉调 MCP，而是在请求转发前由链路确定性地注入 Recent Context / Recalled Memory / Persona State。这是稳定性的分水岭

### 2.2 业界方案速览

| 方案 | 核心抽象 | 对本项目的启示 |
|---|---|---|
| SillyTavern | World Info 关键词触发注入 + 周期性摘要 + 向量 RAG | 三种机制各管一段，注入位置/预算可控；角色设定用"关键词触发的世界书"而非全量塞入 |
| Letta (MemGPT) | core memory（常驻）/ archival（向量库）/ recall（历史检索），agent 自己分页 | 分层思想；但"模型自觉调工具"在陪伴场景不稳定，只借鉴分层、不照搬 tool call |
| Mem0 | 提取原子事实 + ADD-only + 实体关联 + 多信号检索 | 事实层用 ADD-only + 覆盖标记处理矛盾（"她搬家了"不删旧记忆而是标记失效） |
| Zep (Graphiti) | 时序知识图谱，事实带有效期 | 轻量版：事实加 `valid_from/valid_to`，检索时按时间过滤，不需要真上图数据库 |

---

## 3. 总体架构

```
┌─ Vue3 前端（保留，改造为 API 客户端）
│    聊天 UI / 时间线 / 记忆面板（可视化记忆库）
│
┌─ 后端 Node + TS + SQLite（新增，核心）
│    ├─ Chat Gateway（确定性上下文编排器 ★核心）
│    │    每轮请求前自动装配：人设卡 + 关系状态 + 近期窗口 + 检索记忆
│    ├─ Memory Pipeline（异步，不阻塞回复）
│    │    提取 → 校验 → 去重 → 落库 → embedding
│    ├─ Retrieval（混合检索：向量 + 关键词 + 时序，RRF 融合）
│    ├─ Emotion/Relationship State（回复后由 LLM 评估更新）
│    └─ Model Router（对话/提取/评估三级路由，失败降级）
```

**设计原则（面试可讲的决策）**：
1. 记忆注入由编排层**确定性保证**，不依赖模型 tool call 自觉性 → 稳定可测
2. 提取/摘要异步进行，**不阻塞用户对话延迟**
3. ADD-only + 时间有效期，不做破坏性更新 → 记忆可审计、矛盾可解释
4. 规模适配：几百~几万条记忆，SQLite + 暴力余弦足够，**不引入向量数据库**（有量化分析支撑，见 §7）

---

## 4. 记忆分层设计（五层）

| 层 | 内容 | 生命周期 | 注入策略 |
|---|---|---|---|
| L0 人设卡 Core | 角色设定、世界观、说话风格、经典语录 | 手动/半自动演进 | 永远全量注入（<1K token） |
| L1 关系状态 State | 好感度、当前心情（效价/唤醒度）、关系里程碑、纪念日 | 每轮回复后 LLM 评估更新 | 永远全量注入（<500 token） |
| L2 近期窗口 Working | 最近 N 轮原文 | 滚动 | 全量注入 |
| L3 情景记忆 Episodic | 场景级摘要（保留原文引用回链 + 情绪标签） | 遗忘曲线衰减，被回忆时刷新 | 混合检索，按预算注入 |
| L4 事实记忆 Semantic | 原子事实（用户生日/喜好/忌口/重要的人），ADD-only + 有效期 | 长期，矛盾时旧记录标记 valid_to | 向量+实体双索引，按预算注入 |

关键点：
- **L3 防"14 个字"坑**：每场景（session 或每 ~20 轮）做细摘要（150-300 字，含情绪细节与原文 message id 回链）；每 5 个场景再做一次粗摘要。检索命中细摘要时，可按回链取回原文片段。
- **L4 防矛盾**：新事实与旧事实冲突时不删除，旧记录 `valid_to = now`，检索按时间过滤取最新值。面试话术："时间感知的事实有效性，Zep Graphiti 的轻量实现"。
- **遗忘曲线**：记忆带 `importance`（情感权重，提取时由 LLM 打分）与 `last_recalled`；综合得分 = f(情感权重, 距今时间, 被回忆次数)，低分记忆降权不删除（"淡去"而非"删除"，符合情感陪伴调性）。

---

## 5. 提取管道与稳定性（吸收踩坑经验）

```
对话 batch（软边界切割：按场景/话题边界切，不硬切轮数）
  → 提取（便宜模型，如 deepseek-chat）
     产出：场景摘要 / 原子事实 / 情绪标签 / 情感权重
  → 校验层 ★
     - 摘要字数下限（防压缩过度）
     - 产出必须引用 message id（防幻觉、可溯源）
     - 空 batch 结果 → 记日志并重试一次（换模型路由）
  → 语义去重（embedding cosine > 0.92 视为重复，合并引用计数）
  → 落库 + embedding（异步批量）
```

- **软边界切割**：用 LLM 或启发式（话题转换检测）切 batch，避免一句话被切到两个 batch 语义断裂。
- **敏感内容静默跳过**：校验层发现 batch 无产出时，换路由重试；仍失败则标记该 batch 待处理，绝不静默丢弃。
- **三级模型路由**：对话（主力模型）/ 提取摘要（便宜快）/ 状态评估（小模型即可）。任一路径失败有降级路径。

---

## 6. 检索与注入（每轮对话的上下文装配）

```
query = 用户最新消息 + 最近 2 轮
检索三路：
  ① 向量：query embedding vs 记忆 embeddings（SQLite 暴力余弦）
  ② 关键词：BM25/分词命中（用户名、地名、专有名词强信号）
  ③ 时序：近期高情感权重记忆加成
RRF（Reciprocal Rank Fusion）融合 → 去重 → token 预算裁剪（如 2K）
最终 prompt = L0 人设卡 + L1 关系状态 + [检索记忆块] + L2 近期窗口
注入位置固定、预算固定 → 每轮 prompt 结构确定、成本可预算（稳定性卖点）
```

---

## 7. 技术选型（及"为什么不用 X"）

| 组件 | 选型 | 理由 / 面试答法 |
|---|---|---|
| 后端 | Node + TS + Express/Fastify + better-sqlite3 | 与前端同语言，单文件数据库零运维，个人产品规模足够 |
| 存储 | SQLite（消息、记忆、事实、状态各一张表 + FTS5 关键词索引） | 不用向量数据库：1 万条 512 维向量全量余弦 < 10ms 量级（可现场算给面试官看），引入 pgvector/Qdrant 是为规模付不必要的复杂度 |
| Embedding | 智谱/SiliconFlow embedding API（Qwen 系，OpenAI 兼容，国内直连、极便宜） | DeepSeek 无 embedding API；本地 sentence-transformers 需 Python，运维重 |
| LLM | 保留多端点路由，任务分级（对话/提取/评估） | 稳定性 + 成本控制双重理由 |
| 前端 | 保留 Vue3，localStorage 只存 UI 偏好；新增"记忆面板"可视化记忆库 | 记忆可视化既是产品差异化也是 debug 工具 |

---

## 8. 评估与稳定性（面试的技术深度支撑）

自建小 benchmark（参照笔记作者做法）：
- 数据：自己与 bot 的真实对话 + 半合成对话
- 指标：
  - **Recall@K**：构造 50 个"应该被想起"的问题（"我上次说考试的事是哪天？"），对比 滚动窗口 / +摘要 / +混合检索 三种配置
  - **压缩保真度**：摘要是否保留情感细节（人工抽检 + LLM-as-judge）
  - **矛盾处理**：注入矛盾事实对，看模型引用的是否是新值
  - **成本**：每轮平均 token 消耗、检索延迟 p95
- 稳定性：提取任务失败率监控、空产出告警、prompt 结构回归测试

这套"方案 + 实验数据 + 各配置对比"就是面试里最硬的技术叙事。

---

## 9. 面试叙事骨架（STAR）

- **S**：AI 情感陪伴的核心矛盾是人格/记忆连贯性 vs 模型无状态上下文有限；市面产品（含我最初的原型）只有滚动窗口，第 N 轮后角色"失忆"
- **T**：为乙游角色陪伴场景设计一个低成本、可解释、稳定的长期记忆系统
- **A**：
  1. 五层记忆模型（core/state/working/episodic/semantic），借鉴 MemGPT 分层与 Conway 叙事记忆
  2. 提取管道防两个实测坑：压缩过度（分层摘要+原文回链）、敏感内容静默跳过（校验+重试+路由降级）
  3. 混合检索（向量+关键词+时序 RRF 融合），实测优于单一向量
  4. **确定性注入**：记忆由 Gateway 编排层强制带入而非依赖模型自觉 tool call —— 把"会不会想起"从概率问题变成工程保证
  5. 情感特化：关系状态机 + 遗忘曲线 + 情感权重（区别于通用 agent 记忆框架的产品差异化）
- **R**：自建 benchmark 上 Recall@K 相对滚动窗口提升 X%，每轮 token 成本比全量历史降低 Y%，提取失败率 < Z%

---

## 10. 实施路线图

| 阶段 | 内容 | 交付 |
|---|---|---|
| M1 工程底座 | 后端 + SQLite + 多会话 + 角色卡数据化 + 前端接 API | 数据不再怕丢，多角色可扩展 |
| M2 记忆内核 | 提取管道 + 五层存储 + 混合检索 + Gateway 注入 | "记得你"上线 |
| M3 情感层 | 关系状态 + 遗忘曲线 + 记忆面板可视化 | 产品差异化 |
| M4 评估加固 | benchmark + 失败监控 + 降级路径 | 面试数据 |

---

## 附：参考资源

- MemGPT: Packer et al. 2023（分层记忆）
- Lost in the Middle: Liu et al. 2023（长上下文注意力衰减 → 检索优于全量塞入）
- RAG: Lewis et al. 2020
- Conway 自我记忆系统 / McAdams 叙事身份理论（情景记忆的主题化组织）
- SillyTavern World Info / Summarize / Vector Storage 三件套
- Mem0（ADD-only + 多信号检索）、Zep Graphiti（时序事实有效性）
- Ombre Brain（本地记忆 + 情绪坐标 + 遗忘曲线）及其 Gateway 二改（确定性注入 + Persona State + Relationship Weather）
