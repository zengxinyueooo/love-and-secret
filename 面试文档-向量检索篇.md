# 面试文档 · 向量存储与检索篇

> 基于 love-and-secret 记忆系统改造实战经验整理：pgvector + Qwen3-VL-Embedding + 混合检索（向量 + 全文）。

---

## 一、项目背景一句话

我们做了一款乙游男主 AI 陪伴应用的记忆系统，把用户和角色的聊天内容向量化后存进 PostgreSQL + pgvector，通过**向量相似度 + 全文检索 + RRF 融合**做混合召回，最终把最相关的记忆注入大模型 prompt。

---

## 二、高频面试题与回答

### Q1：向量检索的核心指标是什么？怎么权衡？

**答：**

- **召回率（Recall）**：相关文档中被检索回来的比例。召回低 = 漏掉关键记忆。
- **准确率/精确率（Precision）**：检索结果中真正相关的比例。准确低 = 召回一堆噪音。
- **业务上优先召回率**，因为 LLM 对少量噪音不敏感，但漏掉关键上下文会严重答错。
- 二者通常此消彼长，用 **Recall@K / Precision@K / NDCG / MRR** 做离线评估。

---

### Q2：怎么提高向量检索的准确率？

**答：**

1. **换更好的 embedding 模型**
   - 通用场景用 OpenAI `text-embedding-3-large`、BGE、Jina；多模态用 Qwen3-VL-Embedding。
   - 本项目选 **Qwen3-VL-Embedding-8B**，MMEB-V2 多模态 SOTA，支持 MRL 维度压缩。

2. **领域微调 embedding**
   - 通用向量抓不住恋爱陪伴、情绪表达的细微差别，可用对比学习在领域语料上微调。

3. **query 改写 / 扩写**
   - 用户 query 很短时，先让 LLM 生成同义改写、伪文档（HyDE），再做检索。
   - 例：用户说“找工作焦虑”，可扩写成“用户因求职感到焦虑，角色如何安慰”。

4. **重排序（Rerank）**
   - 向量召回 Top-K 后，用 cross-encoder 或 LLM 做二次精排，能显著提升准确率。

5. **混合检索 + RRF 融合**
   - 向量解决语义相似，全文解决关键词精确匹配，二者互补。

6. **维度与索引调优**
   - MRL 模型可输出指定维度（如 1536），在召回和存储之间平衡。
   - pgvector HNSW 索引调整 `m`、`ef_construction`、`ef_search` 参数。

---

### Q3：怎么提高召回率？

**答：**

1. **多路召回**
   - 向量召回 + 全文检索 + 关键词/标签召回，避免单路漏召回。
   - 本项目就是 **向量 + tsquery + ILIKE 子串** 三路兜底。

2. **增大 Top-K**
   - 先把候选池放大到 Top-50/100，再交给 rerank 精选。

3. **查询扩写（Query Expansion）**
   - 用 LLM 生成多个语义变体，分别检索后合并结果。

4. **分块策略优化**
   - 长对话不要一刀切，按语义/时间窗口切分，保证每条记忆语义完整。
   - 本项目按“事件”切分，一条记忆一个情绪事件。

5. **过滤条件不能太早**
   - 先召回再过滤，避免硬性 WHERE 把相关记忆提前筛掉。

6. **负样本训练 / 难例挖掘**
   - 收集检索错误的 bad case，加入对比学习负样本，持续优化 embedding。

---

### Q4：混合检索是怎么做的？为什么要融合？

**答：**

**做法：**

- 向量召回一路，按余弦相似度打分；
- 全文检索一路，按 ts_rank / 子串匹配打分；
- 两路结果用 **RRF（Reciprocal Rank Fusion）** 合并：

```
RRF_score = Σ 1 / (k + rank_i)
```

通常 k=60。两路都命中的文档分数翻倍，自然排在前面。

**为什么融合：**

| 召回方式 | 优势 | 劣势 |
|---|---|---|
| 向量检索 | 语义相似，泛化好 | 对专有名词、ID、短关键词弱 |
| 全文检索 | 关键词精确匹配 | 不理解语义变形、同义词 |

融合后优势互补，是工业界 RAG 标配。

---

### Q5：pgvector 的 IVFFlat 和 HNSW 怎么选？

**答：**

| 维度 | IVFFlat | HNSW |
|---|---|---|
| 构建速度 | 快 | 慢 |
| 查询速度 | 较快 | 快 |
| 召回率 | 中等 | 高 |
| 内存占用 | 低 | 高 |
| 动态插入 | 需周期性重建 | 支持增量 |
| 调参 | lists / probes | m / ef_construction / ef_search |

**选型建议：**

- 数据量小、更新频繁：HNSW 更省事。
- 数据量大、内存敏感：IVFFlat + 定期重建。
- 生产环境常见：**HNSW**，因为召回高、增量友好。

pgvector HNSW 示例：

```sql
CREATE INDEX idx_memories_embedding
ON memories USING hnsw (embedding vector_cosine_ops)
WITH (m = 16, ef_construction = 64);
```

---

### Q6：中文全文检索在 pgvector 里怎么做？踩过什么坑？

**答：**

PostgreSQL 默认 `english` 分词器对中文无效，本项目用了两个兜底：

1. **tsvector 用 `simple` 配置**，按空白和标点分词，至少能处理中英文混合词组；
2. **再加 ILIKE 子串匹配通道**，专门兜底中文语义分词失败的情况。

示例：

```sql
SELECT id, ts_rank_cd(content_tsv, websearch_to_tsquery('simple', $1)) AS rank
FROM memories
WHERE content_tsv @@ websearch_to_tsquery('simple', $1)
   OR content ILIKE '%' || $1 || '%'
```

**踩过的坑：**

- `simple` 对“找工作”不会切分成“找 / 工作”，导致纯中文检索全灭，必须加 ILIKE。
- drizzle 把 `::vector(1536)` 类型修饰符参数化，PG 报 42601 语法错误，要去掉动态维度声明。

---

### Q7：embedding 维度怎么选？MRL 有什么用？

**答：**

- 维度越高，语义表达能力越强，但存储、索引、查询成本越高。
- Qwen3-VL-Embedding 默认 4096 维，支持 **MRL（Matryoshka Representation Learning）**，可以指定输出 1536 / 2048 等维度。
- 本项目统一用 **1536 维**，在召回率和存储成本之间取平衡，与 OpenAI 默认维度对齐，方便以后换模型。
- 实际验证：8 条记忆全部生成 1536 维向量，检索结果合理。

---

### Q8：如何评估检索效果？离线怎么做 benchmark？

**答：**

1. **构造评测集**
   - 准备 (query, 相关记忆 id 列表) 标注对，覆盖常见意图。

2. **离线指标**
   - Recall@K、Precision@K、NDCG@K、MRR。

3. **A/B 对比**
   - 对比不同 embedding 模型、不同融合参数、是否加 rerank。

4. **线上日志**
   - 记录每次检索的 `trace`：query、召回结果、用户反馈、最终 LLM 回复。
   - 用 bad case 驱动迭代。

---

### Q9：多模态 embedding 在记忆系统里有什么用？

**答：**

- 用户可能发图片、截图、表情包给角色，纯文本 embedding 会丢失视觉信息。
- Qwen3-VL-Embedding 支持图文混合输入，能把图片和文字编码到同一向量空间。
- 后续可支持：用户分享照片 → 角色“记住画面内容”并在后续对话中提及。
- 本项目先接入了文本路径，模型已预留图片能力。

---

### Q10：RAG 里检索层常见失败模式有哪些？

**答：**

| 失败模式 | 表现 | 解法 |
|---|---|---|
| 语义漂移 | query 和记忆 wording 不同但语义相关，没召回 | query 改写、领域微调、HyDE |
| 关键词漏召 | 专有名词、ID、时间 | 全文检索 / 标签 / metadata 过滤 |
| 大块截断 | 长记忆语义被切碎 | 语义切分、重叠窗口 |
| 噪音混入 | 召回了无关记忆 | rerank、阈值过滤、负样本训练 |
| 时效性错 | 旧记忆排在新的前面 | 时间衰减、艾宾浩斯遗忘曲线加权 |

---

## 三、项目实战要点（可讲的故事）

1. **选型故事**：为什么选 Qwen3-VL-Embedding？
   - 质量优先：MMEB-V2 多模态 SOTA；
   - 工程可插拔：MRL 支持 1536 维，和 OpenAI 对齐；
   - 价格次要考虑： Embedding 调用成本低，模型质量差异影响更大。

2. **排查故事**：M4 实测时挖出三个连环 bug
   - `embedBatch` 返回对象而非数组，向量全写成 null（假成功）；
   - drizzle 参数化 `::vector(1536)` 触发 PG 42601；
   - `simple` 分词器对中文无效，全文检索全灭。
   - 修复后 8 条记忆全部生成 1536 维向量，混合检索验证通过。

3. **设计故事**：情感层怎么影响检索？
   - 引入关系状态机、情绪坐标、艾宾浩斯遗忘曲线，让检索不仅看语义相似，还看情感相关性和时间新鲜度。

---

## 四、一句话总结

> 向量检索不是“选个好模型就完事”，而是 **embedding 质量 + 索引结构 + 多路召回 + 融合排序 + 领域微调 + 持续 eval** 的系统工程；在本项目中，我们用 pgvector + 混合检索 + 情感权重把这套链路跑通了。
